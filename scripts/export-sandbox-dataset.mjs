#!/usr/bin/env node

console.error("export-sandbox-dataset.mjs was removed during the Azure-only migration.");
console.error("Use Azure API/Postgres-native export tooling instead.");
process.exit(1);
