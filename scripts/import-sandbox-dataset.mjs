#!/usr/bin/env node

console.error("import-sandbox-dataset.mjs was removed during the Azure-only migration.");
console.error("Use Azure API/Postgres-native import tooling instead.");
process.exit(1);
